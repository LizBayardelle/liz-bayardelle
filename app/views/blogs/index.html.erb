<div class="container blog">
	<div class="text-center">
		<h1>Things I Didn't <br class="visible-xs">Use to Know</h1>
		<p>As I stumble through the digital world, <br class="visible-xs">here are some things<br class="hidden-xs">I had to figure out <br class="visible-xs">the hard/laborious/Google-intensive way.</p>

		<div class="row">
			<div id="tag_cloud" class="col-xs-8 col-xs-push-2">
			  <% tag_cloud Blog.tag_counts, %w[s m l] do |tag, css_class| %>
			    <%= link_to tag.name, tag_path(tag.name), class: css_class %>
			  <% end %>
			</div> <!-- tag cloud -->
		</div> <!-- tag cloud row -->

		<% if current_user && current_user.admin %>
			<p><%= link_to "Write New Post", new_blog_path %></p>
		<% end %>
	</div> <!-- center header -->

	<div class="row text-center">
		<hr class="col-xs-6 col-xs-push-3">
	</div>

	<div>
		<% @blogs.each do |blog| %>
			<h2>
				<%= link_to blog.title, blog_path(blog) %>
				<% if current_user && current_user.admin %>
					<%= link_to edit_blog_path(blog) do %>
						<i class="icon ion-ios-compose-outline"></i>
					<% end %>
					<%= link_to blog_path(blog), method: :delete, confirm: "Are you sure you want to delete this blog entry?" do %>
						<i class="icon ion-ios-trash-outline"></i>
					<% end %>
				<% end %>
			</h2>
			<p><%= blog.teaser %></p>
			<p><strong>Tags: <%= raw blog.tag_list.map { |t| link_to t, tag_path(t) }.join(', ') %></strong></p>
			<div class="row">
			    <hr class="col-xs-6 col-xs-push-3">
			</div> <!-- row -->
		<% end %>
	</div> <!-- blogs -->
</div> <!-- page container -->
